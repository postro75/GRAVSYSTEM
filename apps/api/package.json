{
  "name": "@gravsystem/api",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "uvicorn main:app --reload --host 0.0.0.0 --port 8000",
    "start": "uvicorn main:app --host 0.0.0.0 --port 8000",
    "build": "echo 'Python build step: verify imports' && python -c 'import main'",
    "test": "pytest tests -q",
    "type-check": "mypy main.py"
  }
}
